# libreray download if not already done
if(!require(shinythemes)){
  install.packages('shinythemes')
  require(shinythemes)
}
#install.packages("shinythemes")

library(shinythemes)

#theme of the app
#theme= shinytheme('darkly')
theme = shinythemes::themeSelector() #### to see the differentes theme of Shinytheme